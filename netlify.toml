[build]
  # No build command needed for static site
  publish = "."
  
[dev]
  # Specify the directory to serve files from
  publish = "."
  # Use the default port for local development
  port = 8888
  # Enable function debugging
  functions = ".netlify/functions"

[functions]
  # Directory for serverless functions
  directory = ".netlify/functions"